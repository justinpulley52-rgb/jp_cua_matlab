close all
clear all
clc

dt = 0.001;
t = -0.5:dt:0.5-dt;
g = sin(10*2*pi*t);


df = 1;
f = -1/(2*dt):df:1/(2*dt)-df;
G = fftshift(fft(fftshift(g)))*dt;

figure(1)
subplot(2,1,1)
plot(t,g)
axis([-0.5 0.5 -1 1])
grid on

subplot(2,1,2)
plot(f,G)
axis([-500 500 -0 0.6])
